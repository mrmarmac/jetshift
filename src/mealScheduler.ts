import type { Action } from './types';

export function generateMealSchedule(
  wakeTime: string,
  direction: string,
  dayIndex: number,
  opts?: Record<string, unknown>,
): Action[] {
  return [];
}
